#cloud-config
runcmd:
- echo "$(date)" >> /home/rancher/runcmd-log
rancher:
  console: centos
  services:
    user-volumes:
      volumes:
      - /home:/home
      - /opt:/opt
      - /var/lib/kubelet:/var/lib/kubelet
      - /etc/kubernetes:/etc/kubernetes
